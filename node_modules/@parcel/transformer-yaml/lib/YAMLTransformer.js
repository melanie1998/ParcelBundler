"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _plugin() {
  const data = require("@parcel/plugin");

  _plugin = function () {
    return data;
  };

  return data;
}

var _default = new (_plugin().Transformer)({
  async transform({
    asset,
    options
  }) {
    const yaml = await options.packageManager.require('js-yaml', asset.filePath, {
      autoinstall: options.autoinstall
    });
    asset.type = 'js';
    asset.setCode(`module.exports = ${JSON.stringify(yaml.safeLoad((await asset.getCode())), null, 2)};`);
    return [asset];
  }

});

exports.default = _default;